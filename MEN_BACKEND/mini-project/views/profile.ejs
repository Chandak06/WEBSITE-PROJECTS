<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <a
        class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5"
        href="/logout"
        >LogOut</a
      >
      <h3 class="text-3xl mb-5">
        <span class="font-light">Hello </span>, <%= user.name %> 👋
      </h3>
      <h5 class="mb-5">you can create a post</h5>
      <form action="/post" method="post">
        <textarea
          placeholder="what's on your mind"
          class="p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
        ></textarea>
        <input
          type="submit"
          class="cursor-pointer py-2 bg-blue-500 block rounded-md mt-2 w-40 text-sm"
          value="Submit New Post"
        />
      </form>
      <div class="posts mt-20">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="post-container mt-5">
  <% user.posts.reverse().forEach((post) => { %>
    <div class="post w-1/3 p-4 rounded-md border border-zinc-700 bg-zinc-800 mb-4">
      <h4 class="text-blue-500 mb-2"><%= user.username%></h4>
      <p class="text-sm tracking-tight"><%= post.content %></p>
      <!-- <p class="text-xs text-zinc-400 mt-1">
       <%= post.content %>
      </p> -->
      <div class="btns flex justify-evenly mt-3">
        <a class="text-blue-500" href="/like/<%=post_id %>">Like</a>
        <a class="text-zinc-600" href="#">Edit</a>
        <form action="/delete/<%= post._id %>" method="POST">
                <button class="text-red-500 text-sm cursor-pointer">Delete</button>
        </form>

      </div>
    </div>
  <% }); %>
</div>

        </div>
      </div>
    </div>
  </body>
</html>

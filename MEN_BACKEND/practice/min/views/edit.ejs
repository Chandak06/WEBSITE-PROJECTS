<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-900 flex min-h-screen justify-center items-center">
    <div class="bg-zinc-800 max-w-md w-full p-6 rounded-md shadow-md">
      <form action="/update/<%= post._id %>" method="POST">
        <h3 class="text-white font-bold text-xl mb-4 text-center">✏️ Update Post</h3>
        <textarea
          name="content"
          class="w-full outline-none h-40 resize-none rounded-md bg-zinc-700 p-2 text-white"
        ><%= post.content %></textarea>
        <button
          type="submit"
          class="mt-4 w-full text-white font-bold px-4 py-2 bg-yellow-500 rounded-md hover:bg-yellow-600"
        >
          Save Changes
        </button>
      </form> 
    </div>
  </body>
</html>
